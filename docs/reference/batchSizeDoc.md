# Documentation helper for batchSize parameter

Documentation helper for batchSize parameter

## Arguments

- batchSize:

  (\`numeric(1)\`) Number of subjects to process in each batch.
  Default: 1000. Increase for better performance with large datasets,
  decrease if running into memory issues.
