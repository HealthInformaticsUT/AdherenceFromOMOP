% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addFeatures.R
\name{summarisePatientCounts}
\alias{summarisePatientCounts}
\title{Summarise patient counts}
\usage{
summarisePatientCounts(
  adherenceData = NULL,
  drugExposure = NULL,
  cdm = NULL,
  cohort = NULL,
  cohortId = NULL,
  n = 1
)
}
\arguments{
\item{adherenceData}{Database table reference or data frame containing adherence
calculation results, typically generated by [calculateAdherence()] or
[calculateAdherenceSlidingWindow()].}

\item{drugExposure}{Database table reference or data frame containing drug exposure
data with patient information, typically generated by [generateChronicDrugExposure()].
Required columns: person_id, drug_concept_id, drug_exposure_start_date, days_supply.}

\item{cdm}{(`cdm_reference`) A CDM reference object created with CDMConnector.}

\item{cohort}{(`tbl` or `NULL`) Optional cohort table reference for additional counts.}

\item{cohortId}{(`numeric` or `NULL`) Optional vector of cohort_definition_id values.}

\item{n}{(`numeric(1)`) Minimum number of rows per year for a person to be included
in 'Yearly drug exposure record count' table. Default: 1.}
}
\value{
Named list with patient count summaries.
}
\description{
Returns summary statistics about the number of subjects in adherence data
and drug exposure data.
}
\examples{
\dontrun{
cdm <- mockDrugExposure()
conceptSet <- CodelistGenerator::getDrugIngredientCodes(cdm, name = "Atorvastatin")

drugExposure <- generateChronicDrugExposure(cdm = cdm, conceptSet = conceptSet, name = "drug_exposure")
adherenceData <- calculateAdherenceBatched(drugExposure, cdm, cma = "CMA5")

counts <- summarisePatientCounts(adherenceData = adherenceData, drugExposure = drugExposure, cdm = cdm)
}
}
